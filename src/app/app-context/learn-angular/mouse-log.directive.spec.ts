import { MouseLogDirective } from './mouse-log.directive';

describe('MouseLogDirective', () => {
  it('should create an instance', () => {
    const directive = new MouseLogDirective();
    expect(directive).toBeTruthy();
  });
});
