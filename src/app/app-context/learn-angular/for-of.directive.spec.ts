import { ForOfDirective } from './for-of.directive';

describe('ForOfDirective', () => {
  it('should create an instance', () => {
    const directive = new ForOfDirective();
    expect(directive).toBeTruthy();
  });
});
